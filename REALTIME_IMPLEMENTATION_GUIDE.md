# 실제 실시간 트렌드 시스템 구현 가이드

현재 VOX 아카이브 페이지에 적용된 데이터는 정적인(Mock) 데이터입니다. 이를 **실제 실시간 시스템**으로 전환하기 위해서는 **데이터 수집(Backend)**, **저장(Database)**, **전달(Frontend)**의 3단계 파이프라인이 필요합니다.

가장 효율적이고 비용 합리적인 아키텍처인 **Supabase (PostgreSQL + Realtime)** 기반의 구현 방법을 안내해 드립니다.

---

## 1. 시스템 아키텍처 (System Architecture)

```mermaid
graph LR
    A[External APIs] -->|1. Fetch (Cron Job)| B(Supabase Edge Function)
    B -->|2. Upsert Data| C[(Supabase DB)]
    C -->|3. Realtime Event| D[VOX Client (Frontend)]
```

1.  **Data Source**: Google Trends, Twitter API, TikTok API 등에서 데이터를 가져옵니다.
2.  **Edge Function**: 주기적(예: 10분마다)으로 실행되어 데이터를 수집, 정제합니다.
3.  **Database**: 데이터를 저장하고 변동 사항이 생기면 클라이언트에 알림을 보냅니다.
4.  **Client**: 웹소켓을 통해 실시간으로 변경된 숫자를 화면에 반영합니다.

---

## 2. 데이터베이스 설정 (Supabase)

Supabase의 `trends` 테이블을 생성하여 실시간 데이터를 저장합니다.

```sql
-- 테이블 생성
create table realtime_trends (
  id bigint generated by default as identity primary key,
  keyword text unique not null,
  mentions int default 0,
  growth text,
  category text,
  city text,
  updated_at timestamp with time zone default timezone('utc'::text, now())
);

-- Realtime 기능 활성화
alter publication supabase_realtime add table realtime_trends;
```

---

## 3. 데이터 수집 함수 (Edge Function)

외부 API를 호출하거나 크롤링하여 DB를 업데이트하는 서버리스 함수입니다. (예시: Deno/TypeScript)

```typescript
// supabase/functions/fetch-trends/index.ts
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

Deno.serve(async (req) => {
  // 1. 외부 소스에서 트렌드 데이터 가져오기 (예: Google Trends RSS)
  // const trends = await fetchExternalTrends();
  
  // (예시 데이터)
  const newMentions = Math.floor(Math.random() * 10000); 

  // 2. DB 업데이트
  const supabase = createClient(Deno.env.get('SUPABASE_URL'), Deno.env.get('SUPABASE_SERVICE_ROLE_KEY'))
  
  const { data, error } = await supabase
    .from('realtime_trends')
    .upsert({ 
      keyword: '#HyperTexture', 
      mentions: newMentions,
      updated_at: new Date().toISOString()
    })
    .select()

  return new Response(JSON.stringify(data), { headers: { 'Content-Type': 'application/json' } })
})
```
*이 함수를 Supabase의 **Database Cron** 기능을 통해 10분마다 실행되도록 스케줄링합니다.*

---

## 4. 프론트엔드 연동 (Next.js)

`ArchivePage`에서 Supabase 클라이언트를 사용해 실시간 구독을 설정합니다.

```typescript
// app/archive/page.tsx (구현 예시)
import { createClient } from '@/utils/supabase/client';
import { useEffect, useState } from 'react';

// ...

const [liveTrends, setLiveTrends] = useState(initialTrends);

useEffect(() => {
    const supabase = createClient();

    // 'realtime_trends' 테이블의 변경사항을 구독
    const channel = supabase
        .channel('realtime-trends')
        .on(
            'postgres_changes',
            { event: 'UPDATE', schema: 'public', table: 'realtime_trends' },
            (payload) => {
                // 변경된 데이터가 들어오면 상태 업데이트
                const updatedTrend = payload.new;
                setLiveTrends(prev => prev.map(item => 
                    item.keyword === updatedTrend.keyword ? updatedTrend : item
                ));
                
                // (선택) 알림 효과 트리거
                showToast(`${updatedTrend.keyword} 트렌드가 급상승 중입니다!`);
            }
        )
        .subscribe();

    return () => {
        supabase.removeChannel(channel);
    };
}, []);
```

---

## 5. 비용 및 API 고려사항

*   **Google Trends API**: 공식 API는 없으나 `google-trends-api` (Node.js 라이브러리)를 사용할 수 있습니다. 무료지만 레이트 리밋이 있습니다.
*   **SNS APIs (X, Instagram, TikTok)**: 공식 API는 대부분 유료 엔터프라이즈 플랜이 필요합니다.
*   **RSS Feeds**: 뉴스 사이트나 패션 매거진의 RSS 피드를 파싱하는 것이 가장 현실적이고 비용 효율적인 방법입니다.

---

## 🚀 바로 적용해볼 수 있는 대안 (Frontend Simulation)

복잡한 백엔드 구축 없이도 사용자가 "살아있는" 느낌을 받도록 하려면, **클라이언트 사이드 시뮬레이션**을 적용할 수 있습니다.

*   `setInterval`을 사용하여 3~5초마다 `mentions` 숫자가 무작위로 조금씩 증가하도록 설정.
*   숫자가 변할 때 색상이 깜빡이는 **Flash Effect** 추가.
*   배열의 순서를 주기적으로 섞어 순위 변동 연출.

**이 시뮬레이션 방식을 지금 바로 적용해 드릴까요?**
